import Typography from 'typography'

const typography = new Typography({
  baseFontSize: '17px',
  headerFontFamily: ['Segoe UI', 'Helvetica', 'Arial', 'sans-serif'],
  bodyFontFamily: ['Segoe UI', 'Helvetica', 'Arial', 'sans-serif'],
})

export default typography
